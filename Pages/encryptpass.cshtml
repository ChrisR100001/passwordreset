@page
@model PasswordResetPortal.Pages.encryptpassModel
@{

}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Password Encryption</title>
    <script src="/js/tailwind.js"></script>
</head>
<body class="bg-gray-100 text-gray-800">
    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-white shadow-xl rounded-xl p-10 max-w-md w-full space-y-6">
            <h1 class="text-2xl font-bold text-center">Encrypt a Password</h1>

            <form method="post" class="space-y-4">

                <div>
                    <label class="block mb-1 font-medium">Password to Encrypt</label>
                    <input type="password" name="password" required class="w-full border rounded p-2 focus:outline-none focus:ring focus:border-blue-300" />
                </div>

                <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700 transition">
                    Encrypt Password
                </button>
            </form>

            @if (Model.Message != null)
            {
                <div class="text-center text-sm mt-4 font-medium text-red-600">@Model.Message</div>
            }
        </div>
    </div>
</body>
</html>
